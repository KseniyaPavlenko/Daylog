import 'package:daylog/models/user.dart';

class UserService {
  Future<User> me() {
    throw UnimplementedError();
  }

  Future<User> update(User user) {
    throw UnimplementedError();
  }

  Future<void> delete() {
    throw UnimplementedError();
  }
}
